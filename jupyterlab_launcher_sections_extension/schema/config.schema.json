{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["section"],
  "properties": {
    "section": {
      "type": "string",
      "description": "Name of the launcher section to customize"
    },
    "icon": {
      "type": "string",
      "description": "Path to SVG icon file (relative to config dir or absolute)"
    },
    "tooltip": {
      "type": "string",
      "description": "Tooltip text to display on hover"
    }
  },
  "additionalProperties": false
}
